name = "money-tracker-discord"
main = "index.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# 環境變數在 Cloudflare Dashboard 設定（不要寫在這裡避免洩漏）
# 需要設定：
#   DISCORD_PUBLIC_KEY
#   GAS_URL
